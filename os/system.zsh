alias kt='nano ~/.config/kitty/kitty.conf'     
alias xx='exit'

alias x='cd ..'

alias bl='sudo systemctl restart bluetooth'
alias off='systemctl poweroff -i'
alias reb='sudo reboot'

d() {
 reset
}

clc(){
    gnome-calculator &
}

clk(){
    gnome-clocks &
}